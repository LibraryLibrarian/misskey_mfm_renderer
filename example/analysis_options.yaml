analyzer:
  plugins:
    - custom_lint
  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"
    # 新しいFlutterが自動で書き加える除外設定。あらかじめ記載しておく
    - build/**
    - android/**
    - ios/**
    - web/**
    - windows/**
    - macos/**
    - linux/**
include: package:pedantic_mono/analysis_options.yaml

linter:
  rules:
    depend_on_referenced_packages: true
    prefer_relative_imports: true
    always_put_control_body_on_new_line: false
